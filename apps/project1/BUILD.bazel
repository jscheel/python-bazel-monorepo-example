load("@pip_deps//:requirements.bzl", "requirement")

py_binary(
  name = "project1",
  srcs = glob(["project1/**/*.py"]),
  main = 'project1/cli.py',
  deps = [
    "//libs/lib1",
    requirement("humanize")
  ]
)
